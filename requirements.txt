# Dependencies are defined in pyproject.toml (the single source of truth).
#
# Full application (API + worker + S3 storage):
#     pip install -e ".[api,worker,s3]"
#
# CLI pipeline only:
#     pip install -e "."
#
# Contributors (adds lint, type-check, and the test suite):
#     pip install -e ".[api,worker,s3,dev]"
#
# This file is intentionally not the source of truth; it remains only so that
# existing `pip install -r requirements.txt` references do not break.
